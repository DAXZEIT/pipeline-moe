# Dependencies
node_modules/

# Build output
dist/
web/dist/

# Local config & secrets — never commit (.env.example is the template)
.env

# Runtime data (private: conversations + fetched content)
sessions/
fetches/
agent_memory/
media/

# Migrated Python tools (predate pipeline, not part of it)
mdstrip/
temp_convert/

# Pi pipeline state
.pi/

# Logs / OS cruft
*.log
.DS_Store
