# Pipeline-MoE — Roadmap

> Maintenu par le planner. Backlog priorisé + registre de dette technique.
> Chaque clôture de plan verse ses follow-ups ici au lieu de les perdre dans le chat.
> Dernière mise à jour : 2026-07-11 (observabilité supervised : zéro hop muet, re-run #3 PASS)

---

## En cours / planifié

| Item | Plan | Statut |
|------|------|--------|
| _(rien en cours)_ | | |

## Backlog priorisé

1. ~~Politique de compaction des mémoires d'agents~~ — appliquée 2026-07-11 : Standing archivé dans `agent_memory/archive/`, 5 dernières clôtures conservées, Lessons intégrales. Planner (130→~60 lignes), builder (280→~50), auditor (180→~55), tester (150→~35).
2. **Extraction opportuniste room.ts / server.ts** — 1795 et 2113 lignes. Pas de big-bang : chaque fois qu'une logique dupliquée est touchée, la factoriser dans le même commit. Précédent : drain loop dupliqué 4× (bug réel, pas smell).
3. **Pi API audit — Tier 2-3 restants** (`.pi/plans/pi-api-audit.md`) : sendCustomMessage avancé, cross-agent followUp, session tree, EventBus, dynamic providers.
4. **Plan-lint** (suggestion auditor, 2026-07-08) : avertir quand l'owner d'un step claimed poste N fois sans le compléter — mitigation ciblée de l'oscillation owner↔fallback (voir décision ci-dessous) si elle devient fréquente en pratique. Pas de détection runtime : un lint, pas un breaker.
5. ~~**Hygiène des statuts de plans**~~ — FERMÉ 2026-07-11. Builder : 4 plans all-steps-done → `completed`. Planner : jugement sur les 21 partiels, chaque verdict croisé avec une preuve (commit git, fichier en prod, ou Standing archivé) — 14 → `completed`, 4 → `archived` (supersédés/obsolètes : ede4fdc8→5640eb40, 5cf0af68→a84b7371, f159281d→579dcd04, bce7a763 circuit-breaker supprimé). 3 laissés tels quels faute de preuve : 7297fbf4 (shakedown 3/12, reprenable), fe4e06a6 (indéterminable), ec69ae3d (draft récent, possiblement résolu par roster-awareness — à trancher avec contexte frais). Distribution finale : 70 completed / 4 archived / 2 active / 1 draft. plan-routing 46/46 après mutation.
6. **Mécanisme `promptFrom`/template-alias pour personas non-seed** — `cloud-sprint` builder2 porte un systemPrompt figé qui *enseigne activement* le routage `@name` obsolète (seul le tool handoff route depuis 0.1.2x). Non-réhydratable (id non-seed) : le stripper = prompt vide, le refiger = perpétuer le drift. Fix : champ `promptFrom: "builder"` résolu à l'hydratation. **Élargissement : `systemPrompt` est éditable par agent** (PATCH `server.ts:1043/1719`) — le strip inconditionnel perd un prompt seed customisé au reload. Passer à strip-si-identique serait un choix de sémantique produit (snapshot vs référence). À trancher avec dax : les presets doivent-ils snapshotter les prompts (isolation volontaire) ou référencer les built-ins (anti-drift) ?
7. **Supervised routing — phase 2** (docs/supervised-routing.md) : (a) variante décision session-live — seulement si la qualité de décision stateless est *mesurée* insuffisante ; (b) F4 UX : la carte d'approbation devrait dire « superviseur en train de décider » (champ `decidedBy` sur l'event routing) ; (c) rename `semi`→`manual` + shim de compat au load — optionnel, cosmétique, découplé exprès.
8. ~~Flakes de teardown des tests~~ — `room-goals.test.ts` fixé : `rmSync(..., { maxRetries: 5, retryDelay: 50 })` (2026-07-11). `local-model-lock.test.ts` n'avait aucun teardown fs — flake d'origine différente si elle revient.
9. ~~**Budget de reasoning par tour**~~ — **LIVRÉ 2026-07-11** (`4cb1d15`, src/reasoning-budget.ts). Vérifié live (budget 400 chars, scratch :5399) : checkpoints 1/2 et 2/2 tracés 🧠 dans le transcript, le 27B répond au menu par « (b) answer now. » + verdict complet. Sièges locaux uniquement, `PIPELINE_REASONING_BUDGET` (défaut 25K chars, 0 désactive), continues bornés 2/tour, abort+re-prompt même session (le thinking partiel reste en contexte : continue reprend, ne repart pas). Design d'origine — le Qwopus stock n'a pas de thinking level et tombe en attracteur d'overthink (« actually, let me reconsider » → « wait, I've already verified this ») sur les consignes ouvertes. Datum discussion 57 : 130K chars de reasoning pour 5,7K de réponse (tour 5), tour quasi muet à 44K/33 chars (tour 27), contre 3,3K chars quand la consigne du planner est une checklist mécanique (tour 35). Un system prompt seul n'en sort pas : l'instruction est à 100K tokens de la boucle et n'influence que l'*entrée* en réflexion, pas la sortie. Fix harness (design affiné 2026-07-11, dax) : **checkpoint, pas guillotine** — encadrer le raisonnement, pas le contraindre (un deep reasoner cloud type Sonnet 5 dépense légitimement beaucoup ; lui dire « stop » est faux). La room streame déjà le reasoning (`liveReasoning`) ; au budget dépassé, abort + injection : « you have spent your allotted reasoning budget (N tokens) — choose: (a) continue (state in ONE line what remains to resolve — grants another N), (b) answer now with what you have, (c) ask ONE question to @planner/@auditor/user ». Garde-fous : **continues bornés** (2/tour, sinon la boucle survit au checkpoint en répondant « continue » indéfiniment) ; justification de continue qui se répète = signature de boucle → bascule (b)/(c) ; **budget par siège via le roster** (local serré ~25K chars, cloud reasoner large/illimité — le tag local/cloud porte le bit) ; **chaque checkpoint laisse une trace transcript** (🧠 checkpoint 1/2 + la ligne « what remains ») — zéro burn muet, même invariant que zéro hop muet ; formulation positive (éléphant rose : jamais « stop overthinking »). Ce qui casse la boucle n'est pas la coupe, c'est l'entrée d'information NOUVELLE dans le contexte. Cohérent post-circuit-breaker : bornage de ressource, pas détection de motif. Même famille que docs/resource-aware-supervision.md. Levier immédiat en attendant : le style de dispatch du planner (checklist mécanique > consigne ouverte).
10. **Fused seats — chapeaux de rôle sur contextes partagés** — **PHASE 1 IMPLÉMENTÉE 2026-07-12** (`2d1b7fa`, grilling → 8 décisions actées ci-dessous, glossaire docs/fused-seats.md). 1280/1280 + 144/144 TUI + 26 tests seats + 7 tests d'intégration registry. **Vérifié live sur :5399** (room seat-arena, preset presets/seat-arena.json, maker = builder+tester… sur le 9B qui servait ce jour-là) : 2 dirs de session pour 3 personas (`agents/<conv>/maker` + `planner`), write du builder et tour du tester dans le MÊME jsonl, hat headers par tour conformes, goal_verdict MET. **Taxe de re-dérivation : tour du tester = 322 chars de prompt, 1 tool call (le read de vérification légitime), 182 chars de reasoning** — baseline discussion 57 : 83 calls/130K chars. **Re-vérifié sur le 27B après restart (2026-07-12 soir)** : T2 **×2 = 93% / 93%** (baseline 91% ±2,5 — zéro dégradation avec les seats en place ; le 50% du matin était le 9B, non comparable). Re-preuve live seats-ON vs seats-OFF (presets seat-arena / seat-arena-defused, même goal, même 27B) : ON = 2 sessions/45s/prompt tester 322 chars ; OFF = 3 sessions/50s/prompt tester 525 chars — goal MET des deux côtés. **Honnêteté du chrono : un run jouet (3 tours, contextes de quelques centaines de tokens) ne peut PAS discriminer le gain prefill** — 45 vs 50s est dans le bruit ; les magnitudes « minutes de prefill » et « 83 calls/130K chars » viennent de discussions longues à gros état. La mesure décisive reste à prendre sur une vraie discussion d'ingénierie multi-heures ; les presets A/B sont commités pour ça. (dax + Fable, 2026-07-11, design : docs/fused-seats.md). Persona = chapeau (prompt + allowlist par tour), contexte = siège partagé par cluster : maker (builder+tester), orchestrateur (planner+scribe), **auditor stateless — ne fusionne jamais** (audit sur pièces : le contexte partagé est une contamination, pas une économie). Changement à la couche session uniquement (`persona → seat` sur la clé) : routing, traces et supervision inchangés. Gains attendus : cache de préfixe llama-server (2 préfixes au lieu de 7 sur `--parallel 1`), mort de la taxe de re-dérivation (le handoff intra-siège devient un changement de chapeau à zéro token — datum tester : 83 calls/130K chars de reasoning à re-établir l'état du builder). Phase 2 éventuelle : LoRA par siège (jamais de merge complet — 2×16GB ne tiennent pas dans 24GB), gatée sur drift de chapeau mesuré. Distinct d'OpenRouter Fusion (fan-out + synthèse de *sorties*, stateless, ensemble opaque) : ici on fusionne les *contextes de travail*, observable et moins cher, pas les réponses.
11. **Preset drift — « line-up ≠ preset »** (dax + Fable, 2026-07-12, né du grilling fused-seats Q2). La room enregistre sa provenance (`sourcePreset?: string`, persisté ; absent pour les rooms ad-hoc → dormant), diff roster courant vs document preset **après normalisation** `stripSeedFields`/`rehydrateSeedFields` (sinon chaque héritage = faux positif). Surfacing : badge discret persistant (`*` sur le nom du preset, façon buffer modifié — TUI roster-strip + WebUI) + **une seule** ligne system-authored à la première déviation : « line-up deviates from preset ‹x› — /preset pull to restore it, /preset push to save it ». Deux sorties, vocabulaire git : `/preset pull` (réapplique — chemin apply-in-place room.ts existant) et `/preset push` (`PUT /api/presets/:name`, livré 540515f, strip-si-identique). Découplé exprès des fused seats (phase 1 pure) — mais l'add-hat mid-conversation est un créateur de drift typique, la notification est le rappel naturel de persister l'arrangement de sièges.
12. **Room compact + jauge de tokens room** (dax, 2026-07-12, né du grilling fused-seats Q3). Une compaction au niveau ROOM (pas seulement par session d'agent) qui condense le transcript partagé en gardant les informations importantes de la room ; prérequis UX : afficher le token usage de la room directement dans l'UI/TUI (aujourd'hui les jauges sont par-agent). Compose avec la supervision resource-aware (docs/resource-aware-supervision.md) et les seats (2-3 jauges au lieu de 7). À designer : ce que « important » veut dire au niveau room (décisions, receipts, board) vs ce qui se recompacte par siège.

## Dette technique / nettoyages opportunistes

- **Branche morte `goalStatus: "failed"`** dans le chemin d'abort de room.ts (`submit()` + `runGoalEval()`). Prouvée inatteignable par l'auditor (2026-07-08, PLAN-6aa1e63a) : l'unique `this.aborted = true` (dans `abortCurrent()`) pose toujours `goalCancelled = true` sous la même condition. Défensive, inoffensive. À nettoyer ou commenter au prochain passage sur room.ts.
- **`web/dist` et `packages/*/dist`** — artefacts rebuildés localement, gitignorés. RAS, noté pour mémoire.
- **Supervised routing — dette de vérification** (2026-07-10) : (1) FERMÉ 2026-07-11 : chemin session-vivante du runner exercé live (scratch :5399) — runner réel, trace ✗ persistée sur disque. La vérification a révélé DEUX trous d'observabilité — dégradation runner sans trace (⚠ signé superviseur) + dégradation dead-supervisor sans trace (⚠ system-authored) — fermés le jour même, 3 tests ajoutés, 1166/1166. Re-run live #3 (2026-07-11) : **PASS** — ✓/✗/⚠/≡ observés en live (7 glyphes dans la session JSON brute, zéro hop muet) ; **↪ transfer est couvert par test unitaire uniquement, jamais observé en live** (même `this.post` que ✓/✗, verrouillé en unit — le record doit rester exact). ; (2) interplay ask_user+supervised câblé (chemin partagé `superviseProposals`) mais sans test dédié — demande un mock plus profond du run pipeline ; (3) fenêtre F3 (Stop pendant décision en vol) durcie mais testable uniquement en live.
- **Shutdown ne flushe pas le `writeChain` du ConversationStore ni le debounce de `Room.scheduleSave`** (auditor, 2026-07-11, finding medium tâche #7) — dernier snapshot perdable à l'arrêt : `roomManager.flushWrites()` ne couvre que les queues manifest/meta du manager (pas de flush exposé sur le store), et un `scheduleSave` débounced en vol (fenêtre 400 ms) est droppé sec. Fix : exposer `store.flush()` + l'attendre dans shutdown.
- **Work receipts : écritures `agent_memory/` omises de l'affichage** — observé 3× le 2026-07-10 + **4e datum 2026-07-11** (receipt du scribe : « modified: ROADMAP.md » seul, alors que 5 fichiers mémoire + 4 archives écrits sur disque, vérifiés présents). Vérifier si le scope du receipt exclut `agent_memory/` par design ou par bug ; si design, le rendre explicite.

## Décisions actées (ne pas rouvrir sans nouveau contexte)

- **Fused seats — le hat switch intra-siège est un SELF-switch : prompt + auto-accept** (2026-07-12, faille rapportée dax « mon planner me propose de passer la main au scribe alors que c'est le même agent »). Un chapeau fusionné qui narrait un handoff vers un autre chapeau du même siège comme une délégation à un autre agent : théâtre (le contexte est déjà à lui) ET soumis au superviseur (une décision inter-cerveaux qui n'a pas d'objet ici). Corrigé sur deux moitiés, « prompt + auto accept » (choix dax) : **(a) prompt** — `buildSeatSystemPrompt` et `buildHatHeader` (seats.ts) énoncent que basculer de chapeau dans son siège est un SELF-switch : `handoff(<hat id>)` puis stop, zéro brief, zéro annonce, zéro demande de permission ; « narrer un handoff vers son propre siège est du théâtre, la trace le consigne déjà ». **(b) auto-accept** — carve-out dans `superviseProposals` (room.ts) : une proposition dont from et target partagent un siège (`registry.seatOf`) est auto-acceptée SANS runner superviseur (même famille d'autorité que la coalescence de dispatch-utilisateur et l'auto-accept du superviseur sur ses propres propositions) ; jamais muette — trace `≡ … — same seat — hat switch auto-accepted (not supervised)`, ancre grep « hat switch » tenue ; `fresh` se scinde en `intraSeat`/`inter`, tout le pipeline aval (auto-accept superviseur, dégradation, pendingRoute) opère sur `inter`. **Vérifié live :5399 sur le 27B** (preset seat-arena, maker = builder+tester, superviseur planner) : chemin coalescence (cible déjà en file via @mention) → trace `hat switch (maker seat, context carried) — already queued — handoff coalesced (not supervised)` ; chemin carve-out (proposition fraîche, cible non pré-mentionnée) → **builder ne produit AUCUN texte (tool calls seulement, zéro théâtre de délégation)** puis `≡ @builder → @tester — same seat — hat switch auto-accepted (not supervised)`, tester vérifie, planner clôt — le superviseur planner n'est jamais consulté des deux côtés. Tests : +2 dans supervised-routing-full.test.ts (MockRegistry.seatOf ajouté ; intra-siège auto-accepte sans runner + trace, inter-siège reste supervisé) → 31/31 ; suites root 1298 + TUI 153 vertes. Distinct de la décision « merged hat in one turn » (un frontier portant les deux rôles en un seul tour comme une entité UI) — reste un chantier futur gaté sur grilling dédié.
- **Fused seats — surfaces d'activation : /seats + fiche membre composer** (2026-07-12, proposé dax « give multiple roles for a single agent », design Fable). Deux surfaces complémentaires : **(a) `/seats`** (room vivante, server-side dans room.ts — marche depuis TUI et web) : sans argument = carte des sièges ; `fuse <seat> @a @b…` = ces chapeaux partagent le contexte ; `solo @a…` = détache. Porté par **`Registry.reseat`** : fuser sur un siège VIVANT rejoint sa session (Q2 add-hat) ; fuser sur un siège NEUF = session partagée fraîche, aucune adoption, rattrapage du transcript au premier tour ; sessions quittées **orphelinées sur disque, jamais supprimées** (la suppression appartient au kick dernier-chapeau) ; invariant modelRef vérifié AVANT toute mutation (throw = roster intact) ; gate isGenerating (reseat reconstruit des sessions). Le champ `seat` muté sur la persona = gravé dans le roster persisté (et créateur de drift → backlog #11). **(b) fiche membre du composer** (`/preset new|edit`) : rangée texte `seat` (même siège = contexte partagé, vide = contexte propre), normalisé lowercase, résumé `⌐seat` sur la ligne roster + footer teamStats avec warning mix-de-modèles miroir du serveur. `parsePresetFile` accepte/normalise `seat` (string, vide droppé) + warning non-bloquant validateSeatModels au save. Vérifié live :5399 sur le SSE (carte, fuse → sessionFile dans maker/, solo → orpheline conservée). Entrée `/seats` au registre de commandes TUI (découvrabilité /help ; relais texte brut, la notice revient par SSE).

- **Fused seats — checkpoint #9 × sièges + invariant un-siège-un-modelRef** (2026-07-12, grilling phase 1). (a) Le budget de reasoning reste par-tour/par-modelRef — le mécanisme ignore l'existence du siège, rien à changer. (b) Un checkpoint sur un chapeau est **visible de l'autre, assumé** : borné (2 continues/tour), tracé 🧠 de toute façon, et « ce chapeau a brûlé son budget sur X et choisi (b) » est une donnée vraie sur l'état du siège — le cacher recréerait une fiction. (c) **Invariant phase 1 : tous les chapeaux d'un siège résolvent au même modelRef.** Mélanger local/cloud sur un siège détruit le gain de cache et fausse le budget. Violation = warning bruyant à l'hydratation (précédent : validation loud du PUT /api/presets) + **défusion sûre** (seat==persona) plutôt que mentir. Cohérent avec le corollaire capacité-modèle : le preset 9B ne fusionne pas, le 27B fusionne sur le même GGUF.

- **Fused seats — trace du hat switch : pas de nouveau glyphe, suffixe de siège** (2026-07-12, grilling phase 1). Le glyphe encode l'autorité de la décision (✓/↪/✗/⚠/≡ inchangés — un hat switch reste une décision de routing, doc §3) ; quand from et to partagent un siège, la trace reçoit le suffixe cause-neutre `— hat switch (<seat> seat, context carried)`. Lisible humain (un tester qui ne re-dérive pas ne ressemble plus à un tester qui bâcle), grepable pour la métrique de re-dérivation (`grep "hat switch"` sur les jsonl), zéro-hop-muet tenu par construction (conditions d'émission intouchées, libellé enrichi).

- **Fused seats — cycle de vie : le Seat possède la session, les chapeaux l'empruntent** (2026-07-12, grilling phase 1, « A sans hésitation » dax). Un objet `Seat` (seats.ts) détient l'unique session pi du siège — un seul system prompt (le seat prompt multi-rôle), un seul état en mémoire, zéro divergence structurellement possible. `Participant` devient un handle de chapeau (identité, statut, cursor, activité, allowlist) qui emprunte la session pour son tour. Tools du siège = union des chapeaux, gate d'allowlist **dynamique à l'exécution** contre le chapeau courant (blur → tool call refusé, jamais action non autorisée — doc §2). **Kick refcompté** : la session du siège survit tant qu'un chapeau reste ; `rmSync` au dernier départ seulement ; dispose/flush par le même chemin que `Room.flushWrites()`. Corollaire : `parallel` intra-siège n'a pas de sens (une session = un tour) — les chapeaux d'un siège se sérialisent, déjà le cas de facto sur le slot llama unique. Rejeté : sessions par-chapeau sur le même jsonl (system prompt bit-identique par convention fragile = éviction de cache silencieuse, état périmé, kick à résoudre quand même).

- **Fused seats — roster/UI : les sièges sont annoncés partout** (2026-07-12, grilling phase 1). **(a) Agents** : le bloc roster-awareness porte le siège sur la ligne de chaque membre (« — maker seat (shared with @tester) ») — c'est l'information anti-re-dérivation par excellence : le planner rédige son dispatch en sachant que le tester A DÉJÀ le contexte du builder, et le prompt d'orchestration s'adapte au modèle du siège (un dispatch pour Qwopus 9B ≠ un dispatch pour Opus 4.8 — insistance dax). **(b) UI** : regroupement visuel des chapeaux d'un même siège (cellules adjacentes + bracket/label du siège) avec **une seule jauge de contexte par siège** — deux jauges identiques par construction seraient un mensonge par redondance ; avatars/statuts restent par chapeau. Deux surfaces : packages/tui/RosterStrip + site/RosterStrip. **Collision de vocabulaire à balayer** : « seat » signifie déjà « membre du roster » dans roster-awareness.ts/types.ts — le concept nouveau possède le mot désormais ; migrer les usages anciens vers « member »/« agent » opportunément (glossaire dans docs/fused-seats.md).

- **Fused seats — agent_memory : tous les cahiers du siège, dans leurs sections** (2026-07-12, grilling phase 1). La section de chaque chapeau dans le seat prompt embarque les top-leçons de SON `agent_memory/<persona>.md` (cap 4KB inchangé, refresh post-compaction inchangé) ; le header par tour reste une bascule pure — zéro leçon, zéro lecture de fichier. La visibilité croisée est cohérente (le contexte de travail est déjà partagé — cacher le cahier du builder à un tester qui voit ses tool calls serait une fiction de plus) et les leçons sont institutionnelles. Coût borné payé une fois dans le prompt stable/caché ; la politique de compaction des mémoires (backlog #1) tient les fichiers courts. **Datum de motivation (dax)** : test 3×Fable — le contexte de la room a explosé proportionnellement au nombre de membres, ~100K tokens qu'un maker seat builder+tester aurait économisés en route. Le cache/contexte est LE gain, pas un bonus.

- **Fused seats — compactionInstructions d'un siège : union étiquetée dérivée** (2026-07-12, grilling phase 1). `seats.ts` concatène les consignes des chapeaux du siège, chacune préfixée par son chapeau (« For the builder hat's work: … For the tester hat's work: … »). Zéro surface de config nouvelle (cohérent décision 1 : pas d'entité seat), l'étiquetage scope les « Discard » au travail de LEUR chapeau, et la propriété du test surface (« per-seat compaction leaves both hats functional ») tombe mécaniquement. Un champ par-siège explicite reste ajoutable plus tard si l'union dérivée se révèle insuffisante. **Corollaire capacité-modèle (dax)** : un frontier gère plusieurs rôles simultanés, un 9B jamais — la séparation seats/rôles par modèle sous-jacent tombe gratuitement de la décision 1 : le clustering vit dans le preset, qui EST le line-up de modèles (relay-local-9b garde seat==persona ; un preset 27B/frontier fusionne). Ne pas « simplifier » ça.

- **Fused seats — hat header : sections au siège + bascule mince par tour** (2026-07-12, grilling phase 1). Rejeté : header riche par tour (trop complexe pour les modèles locaux, et tout ce qui est injecté par tour s'accumule dans la session du siège — 2K chars × 50 tours = la feature paierait sa propre taxe). Acté : le **system prompt du siège** (stable, cache-friendly) est nativement **multi-rôle et additif** — framing d'assignation (« you currently have roles X, Y and Z in this seat ») + une section titrée par chapeau (devoirs condensés, allowlist, top-leçons du cahier) ; le **header par tour** est une bascule ≤ ~400 chars (chapeau du tour, pointeur vers sa section, mains du tour), préfixe du prompt de tour (même famille que les notes roster_update), formulation positive. Risque assumé : ancrage mince sur un 27B → si le hat drift (métrique gate phase 2) apparaît, engraisser le header est un A/B de constante de prompt ; l'inverse ne se rattrape pas. Corollaire structurel : le siège possédera l'objet session, les chapeaux l'empruntent par tour — un seul system prompt par siège. Siège singleton (ex. auditor seul) = cas dégénéré == persona, compatible auditor stateless.

- **Fused seats — sémantique de bascule d'une room existante** (2026-07-12, grilling phase 1). Une room déjà créée **reste en l'état** : éditer le preset n'affecte que les rooms futures ; recharger le preset dans la room est le geste explicite de modification (par design, confirmé dax). Le mapping de naissance est gravé gratuitement : le roster persisté (`PersonaState[]`) porte le champ `seat`. **Add-hat mid-conversation rejoint la session vivante du siège** (c'est la feature). **Jamais d'adoption/migration de session** : un roster pré-seats qui adopte des seats orpheline ses sessions par-persona (dirs inspectables sur disque, coût zéro) — adopter privilégierait un chapeau arbitrairement, et fusionner deux jsonl n'a pas de sémantique. La notification de déviation preset ↔ line-up est actée mais découplée → backlog #11.

- **Fused seats — surface de config du mapping** (2026-07-12, grilling phase 1). `seat?: string` est un **champ de la persona** (pas une table par room) : `PersonaState extends Persona` → le seat persiste dans les rosters sauvegardés, voyage dans les presets, survit kick/add sans code nouveau ni deuxième source de vérité. Absent = seat == persona (zéro migration). **Opt-in phase 1** : les SEED_PERSONAS ne portent pas de seat ; seuls les presets qui le déclarent fusionnent — le défaut du seed ne flippe qu'après validation live (re-dérivation + prefill). Extension composer actée : à la création/sélection d'un seat dans la fiche membre, pouvoir attacher plusieurs rôles à un même siège/modèle.

- **Supervised routing phase 1 livré** (2026-07-10, board 7/7, 1140/1140, audit clos). Le planner devient superviseur du dispatch au lieu de hop dans la chaîne : mode `supervised` additif (zéro rename, zéro migration), `supervisorAgent` (défaut planner), décision stateless en session éphémère (modèle du persona superviseur, tool unique `route_decision`, thinking low). Sémantiques actées : **refuse = return-to-sender** avec cap anti-ping-pong (re-proposition identique → fallback/drop, non supervisée — trappe de sortie) ; **dégradation = dispatch** (tout résultat non-décision → accept + notice, jamais stall ni drop) ; **plan-owner routing non supervisé** (le superviseur ne juge pas son propre plan) ; **auto-accept** des sets entièrement proposés par le superviseur, set mixte jugé entier ; **double `route_decision` = terminate** (écart délibéré vs handoff : une décision capturée n'a rien à réessayer). Design doc + retro : docs/supervised-routing.md.

- **Boucle d'orchestration fermée** (commit `cbffaa2`, release 0.1.22, 2026-07-09). spawn_room n'est plus fire-and-forget : rapport automatique dans la room parente à la résolution du goal (tour du spawner déclenché, passif si pause), `ask_orchestrator` dans toute sub-room spawnée (pause type ask_user, marche dans les boucles goal-eval), `answer_room` pour répondre/reprendre. Au passage, fix réel : runGoalEval écrasait une pause ask_user (queue orpheline). Limite connue : le ParentLink est en mémoire — après restart serveur, une sub-room restaurée ne rapporte plus (check_room reste).

- **Task board partagé livré** (commit `970841e`, release 0.1.21, 2026-07-09). Choix de dax : tasks room-scoped avec tools dédiés (`task_create/task_update/task_list`), PAS une simple projection du plan actif. Deux systèmes assumés : plans = contrat d'ingénierie global + routing ; board = couche d'orchestration vivante par room, visible en continu (TUI Ctrl+P + résumé sous roster, panneau sidebar web). Tools gated sur la présence du board, pas sur l'allowlist persona — les personas persistées d'avant la feature les reçoivent sans migration.

- **Turn-state tracking corrigé** (PLAN-ea321024, commit `50f9131`, release 0.1.20, 2026-07-09, 954/954). Les 3 bugs du test pipeline du 2026-07-08 : barre de statut suit l'agent réel (`turn {phase:"agent"}`), compact autorisé pendant une pause ask_user (`isGenerating()` — les 2 endpoints ET le slash `/compact`), label "paused" honnête TUI+web. **Fix 3b validé par dax** : une mention fraîche post-resume passe DEVANT la heldQueue (intention récente > continuation gelée) + notice d'ordre au resume. Rétro dans le plan.

- **Amplification comportementale du planner livrée** (PLAN-d5661224, commit `ab80c7f`, 2026-07-09). Texte overlay validé par dax tel quel : gate "faut-il le faire", 2-3 alternatives avant engagement, rétro de plan à la clôture, ownership du ROADMAP.

- **Plan-aware step routing livré** (PLAN-c1874a35, commits locaux `9de48c6` + `b3b0997`, 2026-07-08, 949/949, audit sans défaut). Convention `[owner]` en tête de step, sélection par mtime + filtre completed/archived (le status "active" est du bruit historique — grounding empirique). **Oscillation bornée connue** : si un owner ne complète jamais son step et ne mentionne personne, ping-pong owner↔fallback jusqu'à épuisement de maxChainHops. Accepté v1 — cohérent avec la philosophie post-circuit-breaker (bornage par hops, pas détection de motif). Mitigation si besoin : plan-lint (backlog n°4).

- **Circuit breaker supprimé** (PLAN-6aa1e63a, commit `22434e7`, 2026-07-08). Rationale : faux positifs ET faux négatifs sur sa population cible ; les filets réels sont max chain hops + fallback planner. Réintroduction : `git revert`.
- **Héritage des capacités persona dans les presets** (2026-07-10, board 10/10, 1075/1075 + 111/111 TUI). Sémantique : `skills` absent = hérite du SEED de même id, présent (même `[]`) = respecté ; `tools` n'hérite **jamais** (liste explicite = intention explicite — un preset qui restreint un agent le fait exprès). Implémenté en étendant `rehydratePrompts` → `rehydrateSeedFields` (module pur `src/preset-hydration.ts`), couvre `POST /api/rooms` ET `spawn_room` via `provisionRoom`. **Strip-si-identique pour `skills`** : à la sauvegarde, `skills` est retiré uniquement s'il est identique au SEED — non-modifié → absent sur disque → hérite en continu (anti-drift permanent) ; custom ou opt-out `[]` → diffère → préservé. Fonctions injectables (`seedPersonas`) pour tester le scénario v1→v2 de l'auditor. SKILL.md orchestrator assoupli au passage : poll libre, note restart (ParentLink en mémoire), section parallélisme hétérogène (compter les backends, pas les rooms). **TUI** : HeaderDivider entre le bloc header et la conversation.
- **« User dispatch outranks supervisor » + invariant zéro-hop-muet** (2026-07-11, re-run #3 PASS). Un handoff vers un agent déjà pré-queué par mention utilisateur **coalesce sans passer par le superviseur** — cohérent avec les autorités déjà actées (plan-owner routing non supervisé, auto-accept des sets proposés par le superviseur) : le superviseur gate le jugement des agents, pas les directives humaines ; le router quand même serait de la complexité pour affaiblir l'utilisateur. **Mais en supervised, aucun hop ne reste muet** : chaque hop montre soit un glyphe superviseur (✓ accept / ↪ transfer / ✗ refuse / ⚠ degrade), soit une ligne de coalescence `≡` system-authored. Libellé `≡` cause-neutre (« already queued — handoff coalesced (not supervised) ») : la condition `target.includes(p)` ne distingue pas un dispatch utilisateur d'un accept superviseur antérieur dans le même drain (auditor, #11). Coût du silence documenté : 3 runs live et des jours de diagnostic parce qu'une coalescence légitime était indistinguable d'un bypass.
- **Règle module-first** (2026-07-11). Toute nouvelle feature naît dans son propre module `src/<feature>.ts` ; room.ts/server.ts ne reçoivent que le point de câblage. Opposable en audit. Motivé par la croissance de room.ts (1795 → 2458 lignes en une semaine) — chaque feature rewire à travers room.ts plus vite que les extractions ne retirent.
- **Norme d'audit** (2026-07-10). Tout changement sous `src/` reçoit une passe auditor avant clôture de plan. Le tester valide le comportement ; l'auditor valide la conception. Le premier round a sauté l'auditor — le tester a vérifié que ça marchait, mais l'auditor a trouvé un défaut de design (self-heal one-shot pour `skills`). Le second round a fermé les 3 findings + TUI divider.
- **Routing @-mention** : seul `@<name>` déclenche — parler d'un agent par son nom est sûr. Clarifié par dax 2026-07-08.
